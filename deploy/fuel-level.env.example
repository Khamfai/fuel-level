# Copy to /etc/default/fuel-level and edit. Every line is optional.
#TLS_PORT=/dev/ttyUSB0
#TLS_BAUD=9600
# fuel-api base URL (no path). The poller calls /v1/logs and /v1/devices/<site>/heartbeat under it.
#TLS_API_URL=https://fuelms-fuelapi-fbqlk8-0155fc-103-66-238-99.sslip.io
#TLS_API_KEY=
#TLS_SITE_ID=station-1
# Set all three to register this site's device on startup (safe to leave set; "already exists" is fine).
#TLS_DEVICE_NAME=Station 1
#TLS_LAT=13.7563
#TLS_LNG=100.5018
#POLL_INTERVAL=60
